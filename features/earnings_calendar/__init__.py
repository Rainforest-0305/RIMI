# -*- coding: utf-8 -*-
"""RIMI earnings_calendar — 정기보고서(실적) 발표 캘린더 모듈.

격리 규칙: 이 패키지 안에서만 파일을 쓴다. 저장소 루트의 dart_poll/config 는
읽기(import)만 하고, build_impact_benchmark.py / dart_ownership.py 는 import 조차
하지 않는다(락).
"""
